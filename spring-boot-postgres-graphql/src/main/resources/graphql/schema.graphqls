type Query {
    getBookByName(name: String): Book
    getAllBooks: [Book]
    getAuthorById(id: Int): Author
    getAllAuthors: [Author]
}

type Book {
    id: ID
    name: String
    pageCount: Int
    author: Author
}

type Author {
    id: ID
    firstName: String
    lastName: String
}